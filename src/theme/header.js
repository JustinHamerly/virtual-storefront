import { createTheme } from '@mui/material/styles';

const header = createTheme({
  palette: {
    primary: {
      main: '#27789A',
    },
  },
  typography: {
    fontFamily: 'Merienda',
  },
});

export default header