import { createTheme } from '@mui/material/styles';

const categories = createTheme({
  palette: {
    primary: {
      main: '#27789A',
    },
  },
  typography: {
    fontFamily: 'Dosis',
    fontSize: 18,
  },
});

export default categories