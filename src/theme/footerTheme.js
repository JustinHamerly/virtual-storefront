import { createTheme } from '@mui/material/styles';

const footer = createTheme({
  palette: {
    primary: {
      main: '#0f2a36',
    },
  },
  typography: {
    fontFamily: 'Dosis',
    fontSize: 10,
  },
});

export default footer