import { createTheme } from '@mui/material/styles';

const header = createTheme({
  palette: {
    primary: {
      main: '#0f2a36',
    },
  },
  typography: {
    fontFamily: 'Merienda',
  },
});

export default header